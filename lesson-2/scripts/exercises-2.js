// 2a. At a restaurant, you order 1 soup for $10, 3 burgers for $8 each, and 1 icecream for $5. Use JavaScript to calculate the cost of the order.
    // (10 + (3 * 8) + 5) // Output: 39.00

// 2b. You're at a restaurant with two friends (three people in total) and make the same order as in 2a. Calculate how much EACH person pays.
    // (10 + (3 * 8) + 5) / 3 // Output: 13.00 per person.

// 2c. Calculate the total cost of a toaster ($18.50) and two shirts ($7.50) each
    // (1850 + (750 * 2)) / 100; // Output: $33.50

// 2d. Calculate a 10% tax for the total in exercise 2c.
    // ((1850 + (750 * 2)) / 100) * 0.10; // Output: $3.35

// 2e. Calculate a 20% tax for the total in 2c (remember that 1% = 1/100, so 20% = 20/100 = 0.20).
    // ((1850 + (750 * 2)) / 100) * 0.20; // Output: $6.70

// 2j. How do we round a number down in JS?
    // Math.floor(2.8); // Output: 2

// 2k. How do we round a number up in JS?
    // Math.ceil(2.2); // Output: 3


// Challenge Exercises:

    // We'll use JS to convert temperatures from Celsius (°C) to Fahrenheit (°F). The formula is:
        // Fehrenheit = (Celcius * 9 / 5) + 32
        // Celsius = (Fahrenheit - 32) * 5 / 9
    
// 2l. The temperature is 25°C. Calculate the temperature in Fahrenheit.
    // (25 * 9 / 5) + 32; // Output: 77°F

// 2m. The temperature is 86°F. Calcualte the temperature in Celcius.
    // ((86 - 32) * 5 / 9); // Output: 30°C

// 2n. The temperature is -5°C. Calculate the temperature in Fehrenheit.
    // (-5 * 9 / 5) + 32; // Output: 23°F