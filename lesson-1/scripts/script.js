// Main Idea of JS: we give instructions to the computer and it follows them.

alert('hello'); // creates a popup with the text "hello".
alert('Good job!'); // creates a popup with the text "Good job!".
// alert'Good job!); // syntax error: 'Unexpected string' since we left out a bracket.

// Math

2 + 2 // Output: 4
10 - 3 // Output: 7

// JS Example

document.body.innerHTML = 'hello'; // JS is case-sensitive, make sure to include camelCase. The output is that it removes everything from the webpage, and puts the text 'hello', on the website.